<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hnac.hzinfo.modules.sys.dao.AnnexDao">
    <insert id="insert" parameterType="com.hnac.hzinfo.modules.sys.entity.Annex">
        <selectKey keyProperty="fileID" resultType="int" order="AFTER" >
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO t_notice_record_file(
        `savePath`,
        `fileMd5`,
        `fileType`,
        `fileName`,
        `fileSize`
        ) VALUES (
        #{savePath},
        #{fileMd5},
        #{fileType},
        #{fileName},
        #{fileSize}
        )
    </insert>

    <select id="getAnnexPathByID"  resultType="String">
        SELECT f.savePath
        FROM t_notice_record_file f
        WHERE f.fileID = #{0}
    </select>

</mapper>